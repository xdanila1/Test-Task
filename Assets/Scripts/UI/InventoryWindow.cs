using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InventoryWindow : MonoBehaviour
{
    [SerializeField] List<InventoryCell> _itemsUI;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
